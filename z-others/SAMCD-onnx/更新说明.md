# 更新说明
- [x] 2026-06-08：初始版本 pipeline
- [x] 2026-06-23：新增带图像配准的推理 pipeline  
  - 在原版 ONNX 两阶段推理（FastSAM + CD Head）基础上，接入了crop_pasteback 配准流程：推理前对 A/B 做 ECC 对齐，裁剪有效 ROI 后推理，再将变化 mask 贴回全图并输出。
  - 修改 `config/default.yaml` 与 `1-inference_onnx.sh` 中的路径后执行 `bash 1-inference_onnx.sh`。
